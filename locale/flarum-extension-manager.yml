

flarum-extension-manager:
    admin:
        auth_config:
            add_modal:
                submit_button: 'إرسال '
        composer:
            repositories:
                add_modal:
                    submit_button: 'إرسال '
        settings:
            title: الاعدادات
